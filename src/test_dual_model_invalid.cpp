#include "dual_model_file.hpp"
#include <iterator>
#include <cstdio>
using namespace tao::dual;
int main(){try{Config c;c.layers=1;c.d=3;c.s=2;c.m=2;c.e=4;c.vocab=261;CpuModel m(c);save_model(m,"build/invalid_fixture_valid.dsm");std::ifstream f("build/invalid_fixture_valid.dsm",std::ios::binary);std::string bytes((std::istreambuf_iterator<char>(f)),{});size_t count=0;auto reject=[&](std::string b){std::ofstream o("build/invalid_fixture.dsm",std::ios::binary);o.write(b.data(),b.size());o.close();bool yes=false;try{auto model=load_model("build/invalid_fixture.dsm");}catch(const std::exception&){yes=true;}if(!yes)throw std::runtime_error("accepted malformed");++count;};for(size_t n:{size_t(0),size_t(3),size_t(27),size_t(31),bytes.size()-1})reject(bytes.substr(0,n));auto b=bytes;b[0]='X';reject(b);b=bytes;b.push_back('X');reject(b);b=bytes;b[4]=65;reject(b);b=bytes;for(int i=28;i<32;++i)b[i]=0;reject(b);b=bytes;b[28]=0;b[29]=0;b[30]=char(128);b[31]=char(127);reject(b);b=bytes;b[32]=3;reject(b);b=bytes;b[32]=64;reject(b);auto loaded=load_model("build/invalid_fixture_valid.dsm");if(loaded.w!=m.w)throw std::runtime_error("valid zero");printf("PASS malformed_cases=%zu valid_zero_row/padding_roundtrip\n",count);return 0;}catch(const std::exception&e){printf("FAIL %s\n",e.what());return 1;}}
