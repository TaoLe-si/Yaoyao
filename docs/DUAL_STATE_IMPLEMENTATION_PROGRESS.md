# 集成实现：共享张量目录

dual_state_config.hpp定义已批准维度与稳定张量命名/形状/三值标记；embedding只登记一次，输出共享，无多余前馈/读出偏置。目录供CPU/CUDA/导出共用，尚无内存分配、算子或训练。

check_dual_state_schema.cpp验证名字唯一及参数预算。171张量；30146560矩阵权重，39424浮点参数，65536尺度，推理参数载荷7956480字节(不含容器/分词器)，会话状态20480字节。初次测试硬编码尺度总数误写61440，改正65536后重跑通过；是测试常量错误，不改模型形状。

本轮无模型训练、无重复能力测试。下一步按此目录实现CPU单步及对应CUDA图；CPU只参考与解码。配置检查尚仅非零及词表下限，外部容器加载时还需容量/溢出保护。完整规格合并与EOS适配仍未完成，不宣称已经GPU训练可用。
