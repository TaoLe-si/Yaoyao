# Pre-run fixed protocol
New diagnostic split hash of unordered personpair+object with salt919,mod4==0 heldout;all layout/query/noise variants grouped. Not disjoint from earlier benchmark by construction;new split not pristine external validation. Two layouts independent of distractor suffix length0or1;OODlength3 evaluated on heldout groups only. Queryreceiver/sender explicit finaltoken. Fixed wide cyclic chain64,embeddings random,not trained. Readout64->24tanh->8;1760parameters.
FP64baseline versus from-start ternary QAT:effective matrices nearest{-scale,0,+scale},scale.25firstlayer,.5secondlayer;biases FP64. FP64 master weights,identity STE inside[-scale,scale],clip updated matrix masters to that interval,biases unbounded. Fixed scales,not learned.1200stepsSGDlr.15,L2.001. No test selection/earlystop. Seeds42/123/2026. Norm1/sqrt6 allinputs. NativeCPU experiment,not full language training nor optimized packed inference.
