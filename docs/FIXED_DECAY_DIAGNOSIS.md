# Fixed decay screening, not general SSM evaluation

Native diagnose_decay_memory.cpp evaluates explicit onehotowner state m=lambda*m+owner_code with argmax readout. lambda0,.5,.9,.99,.999,1;noise0,1,8,64,512,4096. This deliberately removes object identity/query binding:irrelevant events indistinguishable from relevant updates in model input. Therefore failure is EXPECTED from missing relevance representation,not evidence all decay/SSM/learned recurrent models fail. No trained readout or context embedding.

Forlambda<1 recent relevant update distinguishesAB/BA;adding one unrelatedowner event makes argmaxfollowthat event (0percent). lambda1 additive ties and later noise dominance.5timescales plus oracle selecting bestsinglechannel also0percent atnoise8+,but joint nonlinear cross-channel readout untested and cannot be ruledout.

Analytic:retained impulse lambda^n;for0<lambda<1 tends0. Zero-input4096 steps lambda.9 residual3.778e-188,.99 1.324e-18,.999 .016605. In exactreal arithmetic value nonzero,not information-theoretic collision;finiteprecision/noise/readoutconditioning matters. Scalar swap difference proportional1-lambda:slower decay reduces temporalcontrast while improving retention. Lambda0 noninvertible;positive lambda theoretically reversible giveninput but inversion can amplifyerrors. No CPUtiming performed because correctness scope inadequate to justify scaling benchmark.

Conclusion:time-only decay does not supply task relevance by itself. Retain decay/multiscale as possible components;do NOT reject family or claim proof gate mandatory. Need context-conditioned input/readout to compare fairly with previous gate. This screen establishes design requirement,not adopted architecture or trained language model. Previous stablelocal impossibility proof remains stronger and distinct. Next promising direction shared content-sensitive update with identity-preserving skip,or richer recurrent interactions;avoid owneronlytoy as finalcomparison.
