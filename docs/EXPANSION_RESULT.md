# 写入前残差升降维测试

原token最终答案监督，未加入类型或边界监督。长期状态8类不变，局部窗口4token不变。新增16->32tanh->16tanh残差处理：h0=tanh(input),u=tanh(Uh0+b),h=tanh(h0+Vu+c)，再使用原门和内容头。2137参数；直接加宽32隐藏层2121参数；原基线1065。残差、深度、非线性与维度共同变化，不单独归因升维。初始输入/原头参数与原基线共用随机前缀，新增层继续采样；宽基线参数布局不同。

相同allowed pair留出、在线3000Adam步batch32lr.003L2.0001/init2。升维版6411梯度坐标检查误差4.11022271773e-10。42/123/2026升维版全部seen/heldout3格式noise2/32硬软100%。近参数直接宽版seen100但heldout硬~87..100，软~82..100。原共享16 baseline种子123heldout72..79，另两种子hard100。诊断集已多轮使用，不是最终独立验收。

升维带来至少1024稠密MAC/token（16*32+32*16）及额外48tanh，不能因为长期状态没扩大就称CPU免费。存储新增1072FP64参数8576bytes，总17096bytes；未实现专用硬路径计时，未QAT。与直接宽版参数接近但计算不等价。需要残差无升维/等深度对照、新数据种子和heldout分组检验、因果硬路径性能。

追加999全部正确；追加7训练与seen全部正确，但heldout只有73.05%–87.5%。因此5个初始化中4个通过所有条件，1个组合泛化失败；不能称稳健解决。保留所有失败，不以本轮局部成功宣布通用LLM解决。输出diagnose_expand_write4.cpp,optimize_expand_write4.cpp,test_expand_gradient.cpp及wide对照。构造无边界oracle，比先前辅助提交路线的监督条件更接近原目标。
