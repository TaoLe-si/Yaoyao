# Greedy resident integrated

Eight lifecycle cases passed exact predictions/emittedIDs/end/fresh/state bits against independent full-logit reference. Includes empty259,continuation,explicitreset,260reset,freshafterEOS,64cap,259at64,actualgreedy.

Independent resident compiled and actual3reply/reset smoke: replies and token accounting match. Baseline decode404/440/442tps vs greedy446/444/354: inconsistent actual-resident gain; earlier pairedloop+3.4percent does not establish robust interactive speedup. Keep optional greedy API, do not overwrite existing executables. seconds= additional field is total request wall timing; ttft includes tokenization; decode_tps uses n-1 first-to-last intervals.

No 4k claim, no modelmathchanges. GPUtraining untouched.
