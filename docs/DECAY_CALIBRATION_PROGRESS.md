# 门校准偏移压力测试

用户明确要求继续，原目标因20轮额度耗尽处于runtime round-limit状态，已扩至30并resume；未修改原目标为更容易完成的任务。

test_decay_calibration冻结5个共享原token init2模型，无oracle边界。测试RNG632871，每种子64序列，每侧256无关事件；额外改变最后相关owner，答案必须改变。对原始门logit加bias=-2,-1,0,1,2。模拟校准误差，非真实语言噪声/量化误差模型。三个更新式mode0原耦合，3仅写入平方，4p<.5精确拒绝且保持。数据各条件一致，同一批不是独立重复；长长度排除原训练序列重复。

bias -2:原答案/更新 mode0 320/316，mode3 248/261，mode4 300/291，分母320。
bias -1:三方案全部320/320。
bias 0:mode0 319/317，mode3和4全部320/320。新更长样本揭示原基线不再满分，不能沿用旧测试100%承诺。
bias +1:mode0 35/36，mode3 37/35，mode4 320/320。
bias +2:mode0 39/34，mode3 37/34，mode4 162/164。

解释：精确拒绝对正向偏移引起的小幅误写累积有帮助，但负向偏移下会漏掉必要更新；偏移更大时仍失败。p平方不能阻止保留系数1-p持续衰减旧记忆，即便写入被压小；质量bias+1仅.180。mode0/4质量始终1，但概率归一并不保证语义保留。

决定：重点保留“抑制弱写入时同时保持旧记忆”的耦合逻辑。不因本批结果任意调阈值或声称通用抗扰动。下一轮可测归一化p平方更新r=1-p²,w=p²，将输入抑制与旧保持一致，区分精确零拒绝的必要性；同时加入负偏移必要更新，不只优化干扰保持。未新增独立门训练，正式模型未修改。目标active。
