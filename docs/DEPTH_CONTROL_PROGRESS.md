# 等深度无升维对照

用户授权持续迭代、不要求轮数。runtime要求有限max_goal_rounds，已提高100并resume原暂停目标，不表示承诺100轮或无限执行。

diagnose_deep16_write4：保持残差2层处理，但16->16->16取代16->32->16。1609参数 vs升维2137/直接宽2121/原1065。新增512MAC与32tanh vs升维1024MAC/48tanh。长期记忆不变。残差路径、层数、输入监督训练方案一致；中间宽改变仍影响参数初始化采样布局，非完美隔离所有因素。

梯度4827坐标最大3.64536137387e-10。相同在线3000步32batch/init2/严格8配对留出。seeds123/2026已见未见3格式noise2/32全部100；seed7seen100，未见混合97.656%、reverse92.969%、separator100。升维seed7相同条件73..87.5，所以升维不单调更好；无升维加深也能修复原123失败。

解释：目前支持更深残差非线性处理有益，不能声称中间升维是唯一原因。尚未完成5种子深层对照，新独立验证数据与硬路径CPU计时仍待做。所有测试当前是复用诊断集，不能据此宣布普适组合泛化。正式模型不改。源diagnose_deep16_write4.cpp,optimize_deep16_write4.cpp,test_deep16_gradient.cpp。目标active。
