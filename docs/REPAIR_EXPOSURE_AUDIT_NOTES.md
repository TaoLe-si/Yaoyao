# Independent pilot exposure checker

New audit_repair_exposure.mjs replays exact recorded document order and original4slot*8round*256 planning without neuralcompute. Checks each completed loggedupdate supervised/position counts exactly. Predicts40update total and docs touched; once bothmanifests exist reports documentoverlap. This avoids relying solely on trainer own counters.

Fixedarm first20updates match replay. Expected40:224383positions140471supervised721docstouched (some partial). Shufflearm not yetstarted at this check. No claim equaltrainingdata for equalupdates.
