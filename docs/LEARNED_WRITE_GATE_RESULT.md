# Supervised contextual write gate mechanism check
Native diagnose_learned_write_gate.cpp. IMPORTANT task changed from raw token chain:structured event(object,owner) and queryobject supplied,query known beforestream. No NLP parsing or learned event extraction. Gate8onehotinputs(query4,eventobject4)->8tanh->1sigmoid81FP64params. Trained on ALL16query/object pairs with explicit equality/relevance supervision,10000fullbatch steps lr.2 L2.0001. This is supervised lookup learning on seen pairs,NOT novel objectpair generalization,NOT end-to-end final answer loss.

81coordinate finite differences at one sample beforetraining eachseed,max1.07667433996e-10. Seeds42/123/2026 each16/16 gate pairs learned. Hardgate threshold.5 overwrites scalar owner;softgate blends8onehotowner memory. Gate never observesowner,names therefore cannot affect gate decisions. Owner combinations evaluated but not a learned owner-composition generalization claim.

448sequences perlength:4queryobjects,8initialowners,7different updatedowners,updateyes/no. Initial relevant event,noiseN,optional relevant update,noiseN. N0,1,3,16,64;query fixed prestream. All three seeds hard/soft answeraccuracy100percent everyN. Keepfirst50percent everyN (fails required update);alwayswrite100percent N0,7.1429percent N>0.128maxnoise events;maxsequence130. Cases reuse seen eventtype patterns. No sequence training so do not describe as length extrapolation from trained sequences;composition of supervised gate with programmed memory.

Interpretation:tiny learned context-conditioned gate can protect and overwrite explicit eventmemory for this finite domain. Does NOT prove original chain can learn gating from tokens or loss,unseenqueries,post-hoc questions,multiobjectmemory,ternaryQAT or reversibleupdates. Overwrite intentionally lossy,not integration into reversiblechain. Soft100percent refers argmaxanswers,not exactmemory preservation or arbitrarylength safety. No model/checkpoint/server changes. Next end-to-end answer-only gate training without relevance labels and contextual event extraction remain open.
